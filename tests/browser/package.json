{
  "name": "front-desk-browser-smoke",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Standalone Playwright smoke tests for a deployed Front Desk. Separate package so the root install stays free of browser dependencies.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "playwright test",
    "test:list": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "^22.20.4",
    "typescript": "~5.9"
  }
}
