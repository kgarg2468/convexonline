/** A stat tile or a chart card: white on the paper ground, hairline, 10px radius (design-spec §1). */
export const tileClass = "min-w-0 rounded-[10px] border border-border-1 bg-white px-4 py-3";
export const cardClass = "min-w-0 rounded-[10px] border border-border-1 bg-white p-4";

/** A chart's title states the question and the scope ("Replies per day · last 14 days"). */
export const chartTitleClass = "text-[13px] leading-5 font-medium text-ink-1";

/** 28px KPI value: the one large number of a tile, always tabular, never coloured. */
export const valueClass = "block text-[28px] leading-8 font-semibold tracking-[-0.01em] text-ink-1 tabular-nums";
